export const typeDefs = `
    type SongName {
        id: ID!
        name: String!
        locale: String!
        isDefault: Boolean!
        isOriginal: Boolean!
    }
`;

export const resolvers = {};
