export const typeDefs = `
    type TrackName {
        id: ID!
        name: String!
        locale: String!
        isDefault: Boolean!
        isOriginal: Boolean!
    }
`;

export const resolvers = {};
